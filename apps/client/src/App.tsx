import { useEffect } from 'react';

const App = () => {
  useEffect(() => {
    (async () => {
      // await client.post('trades', transformToFormData({ takeProfit: 20 }));
    })();
  }, []);

  return <></>;
};

export default App;
